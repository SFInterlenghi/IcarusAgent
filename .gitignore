# Secrets
.env

# Generated database (rebuildable via ETL)
*.sqlite
*.db
# ...except the production KB, which is committed so the deployed app
# has a knowledge base without re-running ETL on every cold start.
!data/icarus_kb.sqlite

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/

# OS
.DS_Store
Thumbs.db
